#pragma once


using namespace std;



struct PathFinder {


    /*
    
    main needs
    - local path finding for nearby ai's
    - group path finding for battle squadrans
    - group path finding for citizens in cities
    - maybe animals following a player
    - local ai movement to handle ai's bumping into each other (only done locally around player)
    - space pathfinding, which might only need to be localish avoidance
    
    */


    void pathFindTerrain(shared_ptr<Chunk> root_chunk) {

    }

    void 


};